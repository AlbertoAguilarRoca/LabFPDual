
public class ExOnce {

	public static String backAround(String cadena) {
		 String nuevaPalabra="";
		 return nuevaPalabra = cadena.charAt(cadena.length()-1) 
				  + cadena + cadena.charAt(cadena.length()-1);
	}
	
}
